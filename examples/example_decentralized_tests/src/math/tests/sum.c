#include <stdio.h>
#include <../math.h>

int main() {
    int x = soma(10, 10);
    if (a != 20) {
        printf("Not Ok\n");
        return 0;
    }
    printf("Ok\n");

    int y = mult(10, 10);
    if (a != 100) {
        printf("Not Ok\n");
        return 0;
    }
    printf("Ok\n");
    return 1;
}